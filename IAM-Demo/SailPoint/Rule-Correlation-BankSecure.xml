<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule name="Rule-Correlation-BankSecure" type="Correlation">
<Description>
Corrèle un compte applicatif avec une Identity SailPoint.
Recherche par sAMAccountName → attribut name de l'Identity.
Retourne null si aucune correspondance → SailPoint crée une nouvelle Identity.
</Description>
<Source>
    String login  = (String) account.getAttribute("sAMAccountName");
    Filter filter = Filter.eq("name", login);
    Iterator ite  = context.search(Identity.class, filter);
    if (ite != null && ite.hasNext()) {
        Identity identity = (Identity) ite.next();
        return identity;
    }
    return null;
</Source>
</Rule>
